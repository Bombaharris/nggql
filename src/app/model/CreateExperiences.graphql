mutation CreateExperiences($input: [ExperienceCreateInput!]!) {
  createExperiences(input: $input) {
    experiences {
      id
      ...AllExperienceFields
      person {
        name
      }
    }
  }
}
