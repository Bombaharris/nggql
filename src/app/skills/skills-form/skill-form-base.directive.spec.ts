import { SkillFormBaseDirective } from './skill-form-base.directive';

describe('SkillFormBaseDirective', () => {
  it('should create an instance', () => {
    const directive = new SkillFormBaseDirective();
    expect(directive).toBeTruthy();
  });
});
